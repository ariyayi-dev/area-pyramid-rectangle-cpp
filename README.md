#include <iostream>
using namespace std;
int main() {
    int a, b;
    cout << "Enter the length value: ";
    cin >> a;
    cout << "Enter the width value: ";
    cin >> b;

    cout << "area = " << a * b << endl;
    cout << "perimeter = " << 2 * (a + b) << endl;
     
     return 0;
}
